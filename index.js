/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';
import Environment from './src/constants/Environment';

AppRegistry.registerComponent(Environment.NAME, () => App);
